@{
    Layout = "master.cshtml";
    ViewBag.Title = "Login";
}

<script>
  const returnUrl = '@(Request.Query["returnUrl"])';
  const errorMsg = '@ViewBag.AuthenticationError';

  mountView(App.Views.Account.loginView(returnUrl, errorMsg));
</script>
